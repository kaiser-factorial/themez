import React from 'react';

export default function Timeline() {
  return (
    <div className="react-component" style={{padding:'0.5rem',border:'1px solid var(--c-border)',background:'var(--c-surface)',color:'var(--c-text)'}}>
      <strong>Timeline (React)</strong>
    </div>
  );
}
