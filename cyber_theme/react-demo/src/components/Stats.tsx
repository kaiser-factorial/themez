import React from 'react';

export default function Stats() {
  return (
    <div style={{border:'1px solid var(--c-border)',padding:'1rem',background:'var(--c-surface)'}}>
      <h3 style={{color:'var(--c-primary)'}}>Stats (React)</h3>
      <p style={{color:'var(--c-secondary)'}}>Placeholder React component for Stats.</p>
    </div>
  );
}
